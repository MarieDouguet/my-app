import React, { useState } from 'react';

const Network = () => {
    const [data, setData] = useState([]);
    
    
    
    return (
        <div>
            {data}
        </div>
    );
};

export default Network;